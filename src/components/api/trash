// import { getUserID } from '../redux/thunksetup/userService'
// import { useSelector } from "react-redux";

// const baseAPI = `${process.env.REACT_APP_ENDPOINT}` // for every use of API_URL there should be additional path
// const userState=useSelector((state)=> state.auth)
// const token = userState.user.data.sessionAuth.accessToken


// const headers = {
//   "Content-Type": "application/json",
//   "Access-Control-Allow-Origin": "*",
//   "x-nuema-customerId": getUserID(),
//   "Authorization": `Bearer ${token}`
// }

// request(url, method="POST", data=null) {
//   url = baseAPI;
//   const options = { headers, method,}
//   if(data) {
//   options.body - JSON.stringify({...data}); return fetch(url, options);}
//   return fetch (url,options);}

// function Services() {
//   return (
//     <div>
      
//     </div>
//   )
// }

// export default Services



{jobData.responsibility}
{jobData.description}